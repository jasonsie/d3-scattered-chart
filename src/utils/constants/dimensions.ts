/**
 * Default chart dimensions and layout constants
 */
export const CHART_DIMENSIONS = {
  DEFAULT_WIDTH: 800,
  DEFAULT_HEIGHT: 600,
  MARGINS: { top: 30, right: 30, bottom: 60, left: 80 },
} as const;

export const FONT_SIZES = {
  AXIS_LABEL: 14,
  TICK_LABEL: 12,
} as const;
